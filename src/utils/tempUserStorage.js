/**
 * @module utils/tempUserStorage
 * @description Temporary in-memory storage for users (e.g., before final registration).
 *
 *  This storage is **volatile** and will be cleared when the app restarts.
 */

const tempUserStorage = {};

export default tempUserStorage;
